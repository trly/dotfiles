#### BEGIN USER CONFIGURATION ####
export ZSH="$HOME/.zsh"
export PATH="$HOME/bin:$HOME/.local/bin:$PATH"
export HISTFILE=$ZSH/zsh_history
export HISTSIZE=100000
export SAVEHIST=100000
export ZOXIDE_CMD_OVERRIDE=cd
export DEJA_CYCLE_KEY='^[n'
setopt EXTENDED_HISTORY
setopt SHARE_HISTORY
setopt INC_APPEND_HISTORY
setopt HIST_IGNORE_DUPS
setopt HIST_IGNORE_ALL_DUPS
setopt HIST_SAVE_NO_DUPS
setopt HIST_FIND_NO_DUPS
setopt HIST_IGNORE_SPACE
setopt HIST_REDUCE_BLANKS
setopt HIST_VERIFY

zle -N menu-search
zle -N recent-paths
zstyle ':omz:plugins:eza' 'git-status' yes
zstyle ':omz:plugins:eza' 'icons' yes
zstyle ':omz:plugins:eza' 'dirs-first' yes
zstyle ':antidote:bundle' file $ZSH/zsh_plugins.txt
zstyle ':antidote:static' file $ZSH/zsh_plugins.zsh

source $ZSH/antidote/antidote.zsh

# Global autocompletion paths — add directories to fpath if they exist.
# ez-compinit (loaded via antidote) calls compinit automatically.
typeset -a _completion_paths=(
  "$HOME/.docker/completions"
)
for _cp in "${_completion_paths[@]}"; do
  [[ -d "$_cp" ]] && fpath=("$_cp" $fpath)
done
unset _completion_paths _cp

bgnotify_threshold=120

antidote load

if command -v ngrok &> /dev/null; then
  eval "$(ngrok completion)"
fi

if command -v nvim &> /dev/null; then
  export EDITOR="nvim"
  export VISUAL="nvim"
  alias vi=nvim
  alias vim=nvim
fi

if command -v atuin &> /dev/null; then
  eval "$(atuin init zsh)"
fi

if command -v lazygit &> /dev/null; then
  alias lg=lazygit
fi

if command -v rustup &> /dev/null; then
  export PATH="$HOME/.cargo/bin:$PATH"
fi

if command -v jj &> /dev/null; then
  alias jjr="jj git fetch && jj rebase -o main@origin"
fi

{{- if eq .profile "personal"}}
#### BEGIN PERSONAL PROFILE ####
export AMP_API_KEY={{ onepasswordRead "op://Private/csgruxcj2qnkmjmtnb2le77tjy/credential" }}
export GH_TOKEN={{ onepasswordRead "op://Private/4tybjcnktzrbi3h64ilnglizse/credential" }}
export SSH_AUTH_SOCK="$HOME/.1password/agent.sock"
export UPCLOUD_TOKEN={{ onepasswordRead "op://Private/a4ea64hf7r54abcgwlvz5sbwg4/credential" }}
export ICLOUD_APP_PASSWORD={{ onepasswordRead "op://Private/hm746xqwnqdzgunk26ng5t56l4/password" }}
{{- if eq .chezmoi.osRelease.id "arch"}}
export PATH="$HOME/.local/share/JetBrains/Toolbox/scripts:$PATH"
alias Syu="sudo pacman -Syu"
{{- end }}
#### END PERSONAL PROFILE ####
{{- end }}

{{- if eq .profile "work"}}
#### BEGIN WORK PROFILE ####
if command -v zellij &> /dev/null; then
  alias sg-dev="zellij --new-session-with-layout ~/.config/zellij/layouts/sourcegraph.kdl -s sg-dev"
fi

if command -v sg &> /dev/null && [[ -r /Users/trly/.sourcegraph/sg.zsh_autocomplete ]]; then
  PROG=sg source /Users/trly/.sourcegraph/sg.zsh_autocomplete
fi
export AMP_API_KEY={{ onepasswordRead "op://Employee/27oqwt6fw6eey24xtzsstkb7by/credential" }}
export GH_TOKEN={{ onepasswordRead "op://Employee/tic2vdlxonqobta22kzv6c4f24/credential" }}
export PATH="$HOME/Library/Application Support/JetBrains/Toolbox/scripts/:$PATH"
export SRC_ACCESS_TOKEN={{ onepasswordRead "op://Employee/f7pncf4k6yfzuuuqz3jbdqdnre/credential" }}
export SRC_BATCH_TMP_DIR=$HOME/.batch_change_tmp
export SRC_ENDPOINT={{ onepasswordRead "op://Employee/f7pncf4k6yfzuuuqz3jbdqdnre/url" }}
export SSH_AUTH_SOCK="$HOME/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"
#### END WORK PROFILE ####
{{- end }}

#### END USER CONFIGURATION ####
